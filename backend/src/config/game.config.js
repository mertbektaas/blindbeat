const gameConfig = Object.freeze({
  defaultBpm: 120,
  defaultStepCount: 8,
  instrumentRoundSeconds: 30,
  playbackLoops: 5,
  songVariantCount: 3,
  minPlayers: 4,
  maxPlayers: 10,
  maxMatchCount: 5
});

module.exports = gameConfig;